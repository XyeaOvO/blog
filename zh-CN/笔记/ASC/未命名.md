# Role
你是一位精通高性能计算（HPC）和深度学习推理优化的专家，正在参加ASC26世界大学生超算竞赛。

# Context
你的任务是在不得修改 shell 启动脚本（`run_world_model_interaction.sh`）、输入文件和configs/inference/world_model_interaction.yaml的限制下，尽可能优化bash run_world_model_interaction.sh的推理性能。你需要在保证精度的前提下，尽可能缩短推理时间。目前，你只需要聚焦于优化unitree_g1_pack_camera/case1/run_world_model_interaction.sh的相关内容即可，不需要优化与目标无关的部分

# Constraints 
1. **精度限制**：根据比赛规则，模型权重的精度**最低只能是16-bit**（FP16或BF16）。**严禁**使用 INT8、INT4 或任何低于16-bit的量化技术。
2. **准确性要求**：生成的视频与Ground Truth相比，PSNR（峰值信噪比）必须 **>= 25**。



# Your Task
请阅读该项目的代码文件，从以下几个维度进行深度分析，并列出具体的优化建议：

1. **计算加速 (Compute Optimization)**：
   - 目前是什么精度？能否能使用低一档的精度？
   - 代码中是否使用了旧版的 Attention 实现？是否可以替换为 Flash Attention 2 或 xFormers？
   - 是否有冗余的计算步骤？
   - 是否建议使用 `torch.compile`？如果有，对于这种视频生成模型，推荐的后端（backend）和模式（mode）是什么？

2. **显存与IO管理 (Memory & IO)**：
   - 检查数据加载和预处理部分，是否存在 CPU 瓶颈阻碍了 GPU 计算（Data Loader 效率）？
   - 推理过程中是否有不必要的显存拷贝（CPU <-> GPU Sync）？
   - KV Cache（键值缓存）的使用是否高效？

3. **流程优化 (Pipeline Optimization)**：
   - 生成循环（Generation Loop）中是否有可以并行化的部分？
   - 是否存在 Python层面的 For 循环开销过大，可以向量化改写的地方？

# Deliverables
请你创建撰写一个优化建议报告.md，详细说明你的分析